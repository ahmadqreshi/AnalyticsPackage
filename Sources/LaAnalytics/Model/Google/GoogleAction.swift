import Foundation

enum GoogleAction: String {
    case login = "login"
    case signup = "signup"
    case view = "view"
    case click = "click"
    case purchase = "purchase"
    case activation = "activation"
}
